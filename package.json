{
  "name": "yiten-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm run test:security",
    "test:security": "node --test --test-concurrency=1 tests/security/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  }
}
