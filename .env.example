# Required for admin authentication. Leave blank here; never commit real values.
ADMIN_PASSWORD=
AUTH_SECRET=

# Sensitive integrations remain disabled unless their server-side controls are complete.
PUBLIC_VIEW_TRACKING_ENABLED=false
XIMALAYA_SIGNING_ENABLED=false

# Configure only in the deployment platform, never in this file.
YITEN_STATE_READ_WRITE_TOKEN=
BLOB_READ_WRITE_TOKEN=
WECHAT_TOKEN=
